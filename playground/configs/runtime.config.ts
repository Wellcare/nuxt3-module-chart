/*
 * ENV variables can be used to replace these values.
 */
export const runtimeConfig = {
    api: {
        baseUrl: 'https://api.mhealthvn.com', // replaced with NUXT_API_BASE_URL
    },
    consola: {
        leval: 3, // replaced with NUXT_CONSOLA_LEVEL
    },
}
